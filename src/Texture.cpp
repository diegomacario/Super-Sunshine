#include "Texture.h"

Texture::Texture(char * textureFile, TextureCoords * coords)
	: textureFile(textureFile)
	, coords(coords)
{ }


	Colour getColour(float u, float v)
	{

	}

	bool loadImage(){
		
	}

